# -*- coding: utf-8 -*-
"""TeleAssessment Report.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1Ssb3liohWtN7FgbURVQNX8kGPkpHA2JI

# Telecommunication Assessment Report

# Business Objective:

 **Customer churn is a big problem for telecommunications companies. Indeed, their annual churn rates are usually higher than 10%. For that reason, they develop strategies to keep as many clients as possible.

 This is a classification project since the variable to be predicted is binary (churn or loyal customer). The goal here is to model churn probability, conditioned on the customer features. telecommunications**


#**Understanding the Business Problem**

**Your target variable is:**

* 1 = Customer will churn

* 0 = Customer will stay

**The company wants to identify customers likely to leave so it can take action (discounts, offers, retention campaigns, customer support, etc.).**

**Two Types of Errors**

**False Positive (FP)**

Model predicts customer will churn, but actually they stay.

* Example:

Predicted: Churn

Actual: Stay

* Consequence:

Company may give an unnecessary retention offer.

Some extra marketing cost.

* Usually not very serious.

**False Negative (FN)**

Model predicts customer will stay, but actually they churn.

* Example:

Predicted: Stay

Actual: Churn

* Consequence:

Customer leaves.

Revenue is lost.

Acquiring a new customer costs much more than retaining an existing one.

This is usually much more expensive.

* Therefore: Prioritize Recall

Recall focuses on minimizing False Negatives.

Recall=
TP+FN
TP
	​


* A high recall means:

**"Of all customers who actually churned, how many did we successfully identify?"**

For churn prediction, missing a churner is usually worse than contacting a few customers unnecessarily.

Example

Suppose:

100 customers actually churn.
Model A
Detects 90 churners
Misses 10 churners

Recall:

100
90
	​

=90%
Model B
Detects 60 churners
Misses 40 churners

Recall:

100
60
	​

=60%

Model A is generally preferred because it catches more at-risk customers.

What Should You Report in Your Project?

Don't report only Recall.

A strong telecom churn project usually reports:

Recall (Primary Metric)
Precision
F1-Score
ROC-AUC
Confusion Matrix
Recommended Statement for Report

Since the objective of telecom churn prediction is to identify as many potential churners as possible, Recall was chosen as the primary evaluation metric. Missing a customer who is likely to churn (False Negative) results in revenue loss, whereas incorrectly targeting a loyal customer (False Positive) generally incurs only a small retention cost. Therefore, maximizing Recall is more important than maximizing Precision.

What Industry Teams Often Use

Many telecom companies evaluate models using:

Recall @ Top K% customers
ROC-AUC
PR-AUC (Precision-Recall AUC)
F1 Score

If your dataset is imbalanced (which churn datasets usually are), ROC-AUC and F1-score should also be included alongside Recall.

For your academic/project report

Primary metric: Recall
Secondary metrics: F1-score and ROC-AUC
Visualization: Confusion Matrix

This combination is usually the most defensible choice for a telecom churn prediction project.

**Requred pakages**
"""

import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns

"""**Loading and reading the dataset.**"""

df=pd.read_excel("/content/Churn (1).xlsx")

"""**Inspecting the dataset's dimensions**

* The Dataset is is having the total 5000 communication.

* Total 21 features are precent in this dataset.
"""

df.shape

"""**Following is the list of columns:**"""

df.columns

"""**Cheking the dataset dimentions.**

# Variable discrption

* state: Categorical, for the 51 states and the District of Columbia.

* Area.code:

*	account.length: how long the account has been active.

*	voice.plan: yes or no, voicemail plan.

*	voice.messages: number of voicemail messages.

* intl.plan: yes or no, international plan.

* intl.mins: minutes customer used service to make international calls.

* intl.calls: total number of international calls.

* intl.charge: total international charge.

* day.mins: minutes customer used service during the day.

* day.calls: total number of calls during the day.

* day.charge: total charge during the day.

* eve.mins: minutes customer used service during the evening.

* eve.calls: total number of calls during the evening.

* eve.charge: total charge during the evening.

* night.mins: minutes customer used service during the night.

* night.calls: total number of calls during the night.

* night.charge: total charge during the night.

* customer.calls: number of calls to customer service.

* churn: Categorical, yes or no. Indicator of whether the customer has left the company (yes or no).
"""

df

df.drop(columns=['Unnamed: 0'],inplace=True)

"""****

# Description

1) loooking at the 75 percentile and maximum value it's clear that there are outliers precent.

* it shows mean,standerd deviation and total count of values that are currently precent in the dataset.
"""

df.describe()

"""#Cheking information about the dataset


"""

df.info()

"""**Its showing no null values precent but lets check once the uniqueness and value counts.**"""

df.isna().sum()

# I have seen the values counts and uniqueness of the state feature and there's no unknown or null value precent
# No imputation nedded.
# Feature is in original categorical form as it should be so no type casting nedded.
print(df['state'].value_counts())
print(df['state'].unique())
print(df['state'].value_counts().sum())

mask = pd.to_numeric(df['state'], errors='coerce').isna() & df['state'].notna()
df.loc[mask]

"""#`area.code`"""

# I have seen the values counts and uniqueness of the area.code feature and there's no unknown or null value precent
# No imputation nedded.
# Feature is in original categorical form as it should be so no type casting nedded.
print(df['area.code'].value_counts())
print(df['area.code'].unique())

mask = pd.to_numeric(df['area.code'], errors='coerce').isna() & df['area.code'].notna()
df.loc[mask]

"""#`account.length`"""

# I have seen the values counts and uniqueness of the account.length feature and there's no unknown or null value precent
# No imputation nedded.
# Feature is in original numric form as it should be so no type casting nedded.
print(df['account.length'].value_counts())
print(df['account.length'].unique())

# no null is here
mask = pd.to_numeric(df['account.length'], errors='coerce').isna() & df['account.length'].notna()
df.loc[mask]

"""The `voice.plan` column is a categorical variable ('yes'/'no') but for numerical analysis like `regplot`, it needs to be converted into a numerical format. We will map 'no' to 0 and 'yes' to 1."""

df['voice.plan_numeric'] = df['voice.plan'].map({'no': 0, 'yes': 1})
display(df[['voice.plan', 'voice.plan_numeric']].head())

# I have seen the values counts and uniqueness of the voice.plan feature and there's no unknown or null value precent
# No imputation nedded.
# Feature is in original categorical form as it should be so no type casting nedded.
print(df['voice.plan_numeric'].value_counts())
print(df['voice.plan_numeric'].unique())

# no null value precent
mask = pd.to_numeric(df['voice.plan_numeric'], errors='coerce').isna() & df['voice.plan'].notna()
df.loc[mask]

# I have seen the values counts and uniqueness of the voice.messages feature and there's no unknown or null value precent
# No imputation nedded.
# Feature is in original numric form as it should be so no type casting nedded.
print(df['voice.messages'].value_counts())
print(df['voice.messages'].unique())

mask = pd.to_numeric(df['voice.messages'], errors='coerce').isna() & df['voice.messages'].notna()
df.loc[mask]

df['intl.plan_numeric'] = df['intl.plan'].map({'no': 0, 'yes': 1})
display(df[['intl.plan', 'intl.plan_numeric']].head())

# I have seen the values counts and uniqueness of the intl.plan feature and there's no unknown or null value precent
# No imputation nedded.
# Feature is in original categorical form as it should be so no type casting nedded.
print(df['intl.plan_numeric'].value_counts())
print(df['intl.plan_numeric'].unique())

mask = pd.to_numeric(df['intl.plan_numeric'], errors='coerce').isna() & df['intl.plan_numeric'].notna()
df.loc[mask]

# I have seen the values counts and uniqueness of the intl.mins feature and there's no unknown or null value precent
# No imputation nedded.
# Feature is in original numric form as it should be so no type casting nedded.
print(df['intl.mins'].value_counts())
print(df['intl.mins'].unique())

# no null values precent
mask = pd.to_numeric(df['intl.mins'], errors='coerce').isna() & df['intl.mins'].notna()
df.loc[mask]

# I have seen the values counts and uniqueness of the intl.calls feature and there's no unknown or null value precent
# No imputation nedded.
# Feature is in original categorical form as it should be so no type casting nedded.
print(df['intl.calls'].value_counts())
print(df['intl.calls'].unique())

mask = pd.to_numeric(df['intl.calls'], errors='coerce').isna() & df['intl.calls'].notna()
df.loc[mask]

# I have seen the values counts and uniqueness of the intl.charge feature and there's no unknown or null value precent
# No imputation nedded.
# Feature is in original numric form as it should be so no type casting nedded.
print(df['intl.charge'].value_counts())
print(df['intl.charge'].unique())

mask = pd.to_numeric(df['intl.charge'], errors='coerce').isna() & df['intl.charge'].notna()
df.loc[mask]

# I have seen the values counts and uniqueness of the day.mins feature and there's no unknown or null value precent
# No imputation nedded.
# Feature is in original numric form as it should be so no type casting nedded.
print(df['day.mins'].value_counts())
print(df['day.mins'].unique())

mask = pd.to_numeric(df['day.mins'], errors='coerce').isna() & df['day.mins'].notna()
df.loc[mask]

# I have seen the values counts and uniqueness of the day.calls feature and there's no unknown or null value precent
# No imputation nedded.
# Feature is in original numric form as it should be so no type casting nedded.
print(df['day.calls'].value_counts())
print(df['day.calls'].unique())

mask = pd.to_numeric(df['day.calls'], errors='coerce').isna() & df['day.calls'].notna()
df.loc[mask]

# I have seen the values counts and uniqueness of the day.charge feature and there's no unknown or null value precent
# No Imputation nedded
# Feature is  not in original numric form as it should be so type casting nedded.
print(df['day.charge'].value_counts())
print(df['day.charge'].unique())
print(df['day.charge'].dtypes)

# 7 nan rows precent.
mask = pd.to_numeric(df['day.charge'], errors='coerce').isna() & df['day.charge'].notna()
df.loc[mask]

# I have seen the values counts and uniqueness of the eve.mins feature and there's no unknown or null value precent
# No imputation nedded.
# Feature is  not in original numric form as it should be so type casting nedded.
print(df['eve.mins'].value_counts())
print(df['eve.mins'].unique())

# 24 Nan rows precent.
mask = pd.to_numeric(df['eve.mins'], errors='coerce').isna() & df['eve.mins'].notna()
df.loc[mask]

# I have seen the values counts and uniqueness of the eve.calls feature and there's no unknown or null value precent
# No imputation nedded.
# Feature is in original numric form as it should be so no type casting nedded.
print(df['eve.calls'].value_counts())
print(df['eve.calls'].unique())

mask = pd.to_numeric(df['eve.calls'], errors='coerce').isna() & df['eve.calls'].notna()
df.loc[mask]

# I have seen the values counts and uniqueness of the eve.charge feature and there's no unknown or null value precent
# No imputation nedded.
# Feature is in original numric form as it should be so no type casting nedded.
print(df['eve.charge'].value_counts())
print(df['eve.charge'].unique())

mask = pd.to_numeric(df['eve.charge'], errors='coerce').isna() & df['eve.charge'].notna()
df.loc[mask]

# I have seen the values counts and uniqueness of the night.mins feature and there's no unknown or null value precent
# No imputation nedded.
# Feature is in original numric form as it should be so no type casting nedded.
print(df['night.mins'].value_counts())
print(df['night.mins'].unique())

mask = pd.to_numeric(df['night.mins'], errors='coerce').isna() & df['night.mins'].notna()
df.loc[mask]

# I have seen the values counts and uniqueness of the night.calls feature and there's no unknown or null value precent
# No imputation nedded.
# Feature is in original numric form as it should be so no type casting nedded.
print(df['night.calls'].value_counts())
print(df['night.calls'].unique())

mask = pd.to_numeric(df['night.calls'], errors='coerce').isna() & df['night.calls'].notna()
df.loc[mask]

# I have seen the values counts and uniqueness of the night.charge feature and there's no unknown or null value precent
# No imputation nedded.
# Feature is in original numric form as it should be so no type casting nedded.
print(df['night.charge'].value_counts())
print(df['night.charge'].unique())

mask = pd.to_numeric(df['night.charge'], errors='coerce').isna() & df['night.charge'].notna()
df.loc[mask]

# I have seen the values counts and uniqueness of the customer.calls feature and there's no unknown or null value precent
# No imputation nedded.
# Feature is in original numric form as it should be so no type casting nedded.
print(df['customer.calls'].value_counts())
print(df['customer.calls'].unique())

mask = pd.to_numeric(df['customer.calls'], errors='coerce').isna() & df['customer.calls'].notna()
df.loc[mask]

df['churn_numeric'] = df['churn'].map({'no': 0, 'yes': 1})
display(df[['churn', 'churn_numeric']].head())

# I have seen the values counts and uniqueness of the churn feature and there's no unknown or null value precent
# No imputation nedded.
# Feature is in original numric form as it should be so no type casting nedded.
print(df['churn_numeric'].value_counts())
print(df['churn_numeric'].unique())

mask = pd.to_numeric(df['churn_numeric'], errors='coerce').isna() & df['churn_numeric'].notna()
df.loc[mask]

"""# Insights:

**No `null` value or `nan` or `unkown` values precent** but there is problem with 2 features that are numric but behaving like object  

# Type casting column name list

1) `eve.mins`

2) `day.charge`

"""

df.dtypes

"""### Column-wise Insights

#### Categorical Features (Object Type):
"""

print('--- state ---')
print(f"Number of unique states: {df['state'].nunique()}")
print(f"Most frequent state: {df['state'].mode()[0]}")
print("Insight: This column represents geographical locations with 51 unique states, indicating a good distribution across different regions. No immediate issues are apparent.")

print('\n--- area.code ---')
print(f"Number of unique area codes: {df['area.code'].nunique()}")
print(f"Most frequent area code: {df['area.code'].mode()[0]}")
print("Insight: There are 3 distinct area codes, which is a categorical feature. This can be one-hot encoded if needed for modeling.")

print('\n--- voice.plan ---')
print(f"Voice plan distribution:\n{df['voice.plan'].value_counts()}")
print("Insight: This is a binary categorical feature ('yes'/'no') indicating whether a customer has a voice plan. It's clean and ready for encoding.")

print('\n--- intl.plan ---')
print(f"International plan distribution:\n{df['intl.plan'].value_counts()}")
print("Insight: Similar to 'voice.plan', this is a binary categorical feature ('yes'/'no') for international plans. It's clean and ready for encoding.")

print('\n--- churn ---')
print(f"Churn distribution:\n{df['churn'].value_counts()}")
print("Insight: This is the target variable, a binary categorical feature ('yes'/'no'). It shows an imbalanced class distribution, with more 'no' (non-churn) instances than 'yes' (churn). This should be considered during model training.")

"""#### Numeric Features (Int64 and Float64 Types):"""

numeric_cols = df.select_dtypes(include=['int64', 'float64']).columns.tolist()

# Exclude 'day.charge' and 'eve.mins' as they were just converted and inspected in detail.
# Also exclude 'Unnamed: 0' if it still exists, but it was dropped earlier.

print('\n--- account.length ---')
print("Insight: Represents the duration of the account in days. The values range from 1 to 243. Box plot analysis showed some outliers, which is expected for such a feature and might be handled later if they significantly impact model performance.")

print('\n--- voice.messages ---')
print("Insight: This column contains the number of voicemail messages, with a significant number of customers having 0 messages. The maximum is 52. Outliers were detected, but they might represent valid higher usage.")

print('\n--- intl.mins, intl.calls, intl.charge ---')
print("Insight: These features describe international usage. 'intl.mins' (0.0 to 20.0), 'intl.calls' (0 to 20), and 'intl.charge' (0.00 to 5.40) show consistent numeric values. Outliers are present in these features, suggesting some customers have higher international usage, which could be a factor in churn prediction.")

print('\n--- day.mins, day.calls, day.charge ---')
print("Insight: These describe day-time usage. 'day.mins' (0.0 to 351.5), 'day.calls' (0 to 165), and 'day.charge' (0.00 to 59.76, after conversion). They cover a wide range of values, and some outliers were noted in the box plots, particularly for calls and charge, indicating varied customer behavior.")

print('\n--- eve.mins, eve.calls, eve.charge ---')
print("Insight: These describe evening usage. 'eve.mins' (0.0 to 363.7, after conversion), 'eve.calls' (0 to 170), and 'eve.charge' (0.00 to 30.91). Similar to day usage, they exhibit a range of values with some outliers indicating high evening usage.")

print('\n--- night.mins, night.calls, night.charge ---')
print("Insight: These describe night-time usage. 'night.mins' (0.0 to 395.0), 'night.calls' (0 to 175), and 'night.charge' (0.00 to 17.77). These also show a spread of values and outliers, reflecting different usage patterns during the night.")

print('\n--- customer.calls ---')
print("Insight: Represents the number of calls made to customer service (0 to 9). The distribution shows that most customers make a low number of customer service calls, but there are outliers with a higher number of calls. This could be a significant predictor for churn.")

"""****"""

df.columns

"""#Cheking for duplicates"""

#print(df.duplicated().sum())
#print(df.loc[df.duplicated().sum()])
print(f"Number of duplicated rows: {df.duplicated().sum()}")
print("Duplicated rows (if any):")
print(df[df.duplicated()])

"""According to the output of the last executed cell, there are no duplicate rows in the dataset. The number of duplicated rows is 0, and the DataFrame showing duplicated rows is empty.

#Outlier detection
"""

from typing import Literal
plt.figure(figsize=(10,5))
plt.boxplot(df['account.length'],vert=False)
plt.title('Outlier detection for account.length')

"""### Insight from `account.length` Boxplot

- **Median (the line inside the box):** This represents the 50th percentile of the data, meaning half of the customers have an `account.length` shorter than this value, and half have a longer one. From `df.describe()`, the median (`50%`) for `account.length` is 100 days.

- **Interquartile Range (IQR - the box itself):** The box spans from the first quartile (Q1, 25th percentile) to the third quartile (Q3, 75th percentile). This range contains the middle 50% of the data, indicating the typical duration of customer accounts.
  - Q1 (`25%`) is 73 days.
  - Q3 (`75%`) is 127 days.
  - The IQR is 127 - 73 = 54 days.

- **Whiskers (lines extending from the box):** These lines typically extend to the minimum and maximum values within 1.5 times the IQR from the quartiles. They show the range of the majority of the data, excluding outliers.

- **Outliers (individual points beyond the whiskers):** These are data points that fall outside the whiskers and are considered unusually low or high values. For `account.length`, we observe several points extending beyond the upper whisker, indicating some customers have significantly longer account durations than the majority. These outliers are present but are not necessarily problematic; they represent valid extreme cases that might be important for analysis (e.g., highly loyal customers).
"""

from typing import Literal
plt.figure(figsize=(10,5))
plt.boxplot(df['voice.messages'],vert=False)
plt.title('Outlier detection for voice.messages')

"""### Insight from `voice.messages` Boxplot

- **Median (the line inside the box):** The median for `voice.messages` is 0. This indicates that at least half of the customers have 0 voicemail messages, suggesting that a significant portion of the customer base either does not use the voicemail feature or does not receive messages.

- **Interquartile Range (IQR - the box itself):**
  - Q1 (`25%`) is 0 messages.
  - Q3 (`75%`) is 17 messages.
  - The IQR is 17 - 0 = 17 messages. This shows that the middle 50% of customers have between 0 and 17 voicemail messages. The fact that Q1 is 0 reinforces the observation that many customers have no voicemail messages.

- **Whiskers (lines extending from the box):** The lower whisker will likely be at 0, given Q1 is 0. The upper whisker would extend up to a certain point, typically Q3 + 1.5 * IQR.

- **Outliers (individual points beyond the whiskers):** The boxplot clearly shows a substantial number of outliers extending to higher values. The `df.describe()` output shows the maximum is 52 messages. These outliers indicate that while many customers have few or no voicemail messages, a subset of customers receives a significantly high number of messages. These are valid data points representing heavy voicemail users and are not necessarily data entry errors.
"""

plt.figure(figsize=(10,5))
plt.boxplot(df['intl.mins'],vert=False)
plt.title('Outlier detection for intl.mins')

"""### Insight from `intl.mins` Boxplot

- **Median (the line inside the box):** The median `intl.mins` is 10.3 minutes. This indicates that half of the customers use 10.3 minutes or less for international calls, and the other half use more.

- **Interquartile Range (IQR - the box itself):**
  - Q1 (`25%`) is 8.5 minutes.
  - Q3 (`75%`) is 12.0 minutes.
  - The IQR is 12.0 - 8.5 = 3.5 minutes. This narrow range suggests that the majority of customers (the middle 50%) have fairly consistent international minute usage, clustering around the median.

- **Whiskers (lines extending from the box):** The whiskers typically cover the data points that are not considered outliers. The minimum value is 0.0 minutes, and the maximum is 20.0 minutes.

- **Outliers (individual points beyond the whiskers):** The boxplot shows outliers on both the lower and upper ends. While the minimum is 0, indicating some customers make no international calls, there are also individual points extending significantly beyond the upper whisker, with a maximum of 20.0 minutes. These outliers represent customers who make a considerably higher number of international calls than the average. These could be high-value customers or those with specific communication needs, and these values are likely valid rather than errors.
"""

plt.figure(figsize=(10,5))
plt.boxplot(df['intl.calls'],vert=False)
plt.title('Outlier detection for intl.calls')

"""### Insight from `intl.calls` Boxplot

- **Median (the line inside the box):** The median `intl.calls` is 4. This indicates that half of the customers make 4 or fewer international calls, and the other half make more.

- **Interquartile Range (IQR - the box itself):**
  - Q1 (`25%`) is 3 calls.
  - Q3 (`75%`) is 6 calls.
  - The IQR is 6 - 3 = 3 calls. This relatively narrow range shows that the central 50% of customers have a consistent, low to moderate number of international calls.

- **Whiskers (lines extending from the box):** The whiskers typically extend to the data points that are not considered outliers. The minimum number of calls is 0, indicating some customers make no international calls, and the maximum is 20.

- **Outliers (individual points beyond the whiskers):** The boxplot clearly shows a significant number of outliers extending to higher values, with the maximum being 20 international calls. These outliers represent a subset of customers who make a considerably higher number of international calls than the majority. These are likely valid data points, reflecting active international users, and are important for understanding the full spectrum of customer behavior.
"""

plt.figure(figsize=(10,5))
plt.boxplot(df['intl.charge'],vert=False)
plt.title('Outlier detection for intl.charge')

"""### Insight from `intl.charge` Boxplot

- **Median the line inside the box** : The median intl.charge is doller 2.78. This indicates that half of the customers are charged $2.78 or less for international calls, and the other half are charged more.

- **Interquartile Range (IQR - the box itself):**
  - Q1 (`25%`) is $2.30.
  - Q3 (`75%`) is $3.24.
  - The IQR is $3.24 - $2.30 = $0.94. This relatively narrow range suggests that the majority of customers (the middle 50%) have fairly consistent international call charges, clustering around the median.

- **Whiskers (lines extending from the box):** The whiskers typically cover the data points that are not considered outliers. The minimum value is 0.00, and the maximum is 5.40.

- **Outliers (individual points beyond the whiskers):** The boxplot shows outliers on both the lower and upper ends. While the minimum charge is 0.00, indicating some customers have no international charges, there are also individual points extending significantly beyond the upper whisker, with a maximum of 5.40. These outliers represent customers who incur considerably higher international charges than the average. These could be high-value customers or those with specific communication needs, and these values are likely valid rather than errors.
"""

plt.figure(figsize=(10,5))
plt.boxplot(df['day.mins'],vert=False)
plt.title('Outlier detection for day.mins')

"""### Insight from `day.mins` Boxplot

- **Median (the line inside the box):** The median `day.mins` is 180.1 minutes. This indicates that half of the customers use 180.1 minutes or less for day-time calls, and the other half use more.

- **Interquartile Range (IQR - the box itself):**
  - Q1 (`25%`) is 143.7 minutes.
  - Q3 (`75%`) is 216.2 minutes.
  - The IQR is 216.2 - 143.7 = 72.5 minutes. This range shows the central 50% of customers have a moderate amount of day-time call usage.

- **Whiskers (lines extending from the box):** The whiskers typically cover the data points that are not considered outliers. The minimum value is 0.0 minutes, indicating some customers make no day-time calls, and the maximum is 351.5 minutes.

- **Outliers (individual points beyond the whiskers):** The boxplot shows outliers on both ends. There are individual points extending significantly beyond both the lower and upper whiskers. The minimum value is 0.0, suggesting some customers have no day-time usage, which is a valid observation. On the upper end, values up to 351.5 minutes indicate customers with exceptionally high day-time call durations. These outliers represent valid extreme usage patterns and not necessarily errors.
"""

plt.figure(figsize=(10,5))
plt.boxplot(df['day.calls'],vert=False)
plt.title('Outlier detection for day.calls')

"""### Insight from `day.calls` Boxplot

- **Median (the line inside the box):** The median `day.calls` is 100. This means that half of the customers make 100 or fewer calls during the day, and the other half make more.

- **Interquartile Range (IQR - the box itself):**
  - Q1 (`25%`) is 87 calls.
  - Q3 (`75%`) is 113 calls.
  - The IQR is 113 - 87 = 26 calls. This relatively tight range indicates that the central 50% of customers have a consistent number of day-time calls.

- **Whiskers (lines extending from the box):** The whiskers extend to values within 1.5 times the IQR from the quartiles. The minimum value is 0 calls, suggesting some customers make no day-time calls, and the maximum is 165 calls.

- **Outliers (individual points beyond the whiskers):** The boxplot shows outliers on both the lower and upper ends. Values at 0 calls are present, indicating some customers do not use their phone during the day. On the upper end, there are individual points extending significantly beyond the upper whisker, with a maximum of 165 calls. These outliers represent customers with exceptionally high day-time call volumes. These are likely valid extreme usage patterns and not errors.
"""

plt.figure(figsize=(10,5))
plt.boxplot(df['eve.calls'],vert=False)
plt.title('Outlier detection for eve.calls')

"""### Insight from `eve.calls` Boxplot

- **Median (the line inside the box):** The median `eve.calls` is 100. This indicates that half of the customers make 100 or fewer calls during the evening, and the other half make more.

- **Interquartile Range (IQR - the box itself):**
  - Q1 (`25%`) is 87 calls.
  - Q3 (`75%`) is 114 calls.
  - The IQR is 114 - 87 = 27 calls. This relatively tight range suggests that the central 50% of customers have a consistent number of evening calls.

- **Whiskers (lines extending from the box):** The whiskers extend to values within 1.5 times the IQR from the quartiles. The minimum value is 0 calls, indicating some customers make no evening calls, and the maximum is 170 calls.

- **Outliers (individual points beyond the whiskers):** The boxplot shows outliers on both the lower and upper ends. Values at 0 calls are present, indicating some customers do not use their phone during the evening. On the upper end, there are individual points extending significantly beyond the upper whisker, with a maximum of 170 calls. These outliers represent customers with exceptionally high evening call volumes, which are likely valid extreme usage patterns and not errors.
"""

plt.figure(figsize=(10,5))
plt.boxplot(df['eve.charge'],vert=False)
plt.title('Outlier detection for eve.charge')

"""### Insight from `eve.charge` Boxplot

- **Median (the  line inside the box):** The median `eve.charge` is $17.09. This means that half of the customers are charged $17.09 or less for evening calls, and the other half are charged more.

- **Interquartile Range (IQR - the box itself):**
  - Q1 (`25%`) is $14.14.
  - Q3 (`75%`) is $19.90.
  - The IQR is $19.90 - $14.14 = $5.76. This range indicates that the central 50% of customers have a moderate amount of evening call charges.

- **Whiskers (lines extending from the box):** The whiskers typically cover the data points that are not considered outliers. The minimum value is $0.00, indicating some customers have no evening charges, and the maximum is $30.91.

- **Outliers (individual points beyond the whiskers):** The boxplot shows outliers on both ends. Values at $0.00 are present, suggesting some customers do not use their phone during the evening, which is a valid observation. On the upper end, values up to $30.91 indicate customers with exceptionally high evening call charges. These outliers represent valid extreme usage patterns and are not necessarily errors.
"""

plt.figure(figsize=(10,5))
plt.boxplot(df['night.mins'],vert=False)
plt.title('Outlier detection for night.mins')

"""### Insight from `night.mins` Boxplot

-   **Median (the line inside the box):** The median `night.mins` is 200.4 minutes. This indicates that half of the customers use 200.4 minutes or less for night-time calls, and the other half use more.

-   **Interquartile Range (IQR - the box itself):**
    -   Q1 (`25%`) is 166.9 minutes.
    -   Q3 (`75%`) is 234.7 minutes.
    -   The IQR is 234.7 - 166.9 = 67.8 minutes. This range indicates that the central 50% of customers have a moderate amount of night-time call usage.

-   **Whiskers (lines extending from the box):** The whiskers typically cover the data points that are not considered outliers. The minimum value is 0.0 minutes, indicating some customers make no night-time calls, and the maximum is 395.0 minutes.

-   **Outliers (individual points beyond the whiskers):** The boxplot shows outliers on both ends. Values at 0.0 minutes are present, suggesting some customers do not use their phone during the night, which is a valid observation. On the upper end, values up to 395.0 minutes indicate customers with exceptionally high night-time call durations. These outliers represent valid extreme usage patterns and are not necessarily errors.
"""

plt.figure(figsize=(10,5))
plt.boxplot(df['night.calls'],vert=False)
plt.title('Outlier detection for night.calls')

"""### Insight from `night.calls` Boxplot

-   **Median (the line inside the box):** The median `night.calls` is 100. This indicates that half of the customers make 100 or fewer calls during the night, and the other half make more.

-   **Interquartile Range (IQR - the box itself):**
    -   Q1 (`25%`) is 87 calls.
    -   Q3 (`75%`) is 113 calls.
    -   The IQR is 113 - 87 = 26 calls. This relatively tight range suggests that the central 50% of customers have a consistent number of night-time calls.

-   **Whiskers (lines extending from the box):** The whiskers extend to values within 1.5 times the IQR from the quartiles. The minimum value is 0 calls, indicating some customers make no night-time calls, and the maximum is 175 calls.

-   **Outliers (individual points beyond the whiskers):** The boxplot shows outliers on both the lower and upper ends. Values at 0 calls are present, suggesting some customers do not use their phone during the night. On the upper end, there are individual points extending significantly beyond the upper whisker, with a maximum of 175 calls. These outliers represent customers with exceptionally high night-time call volumes, which are likely valid extreme usage patterns and not errors.
"""

plt.figure(figsize=(10,5))
plt.boxplot(df['night.charge'],vert=False)
plt.title('Outlier detection for night.charge')

"""### Insight from `night.charge` Boxplot

-   **Median (the line inside the box):** The median `night.charge` is $9.02. This indicates that half of the customers are charged $9.02 or less for night-time calls, and the other half are charged more.

-   **Interquartile Range (IQR - the box itself):**
    -   Q1 (`25%`) is $7.51.
    -   Q3 (`75%`) is $10.56.
    -   The IQR is $10.56 - $7.51 = $3.05. This relatively tight range suggests that the central 50% of customers have consistent night-time call charges.

-   **Whiskers (lines extending from the box):** The whiskers typically cover the data points that are not considered outliers. The minimum value is $0.00, indicating some customers have no night-time charges, and the maximum is $17.77.

-   **Outliers (individual points beyond the whiskers):** The boxplot shows outliers on both ends. Values at $0.00 are present, suggesting some customers do not use their phone during the night, which is a valid observation. On the upper end, values up to $17.77 indicate customers with exceptionally high night-time call charges. These outliers represent valid extreme usage patterns and are not necessarily errors.
"""

plt.figure(figsize=(10,5))
plt.boxplot(df['customer.calls'],vert=False)
plt.title('Outlier detection for customer.calls')

"""### Insight from `customer.calls` Boxplot

-   **Median (the line inside the box):** The median `customer.calls` is 1. This means that half of the customers make 1 or fewer calls to customer service, and the other half make more.

-   **Interquartile Range (IQR - the box itself):**
    -   Q1 (`25%`) is 1 call.
    -   Q3 (`75%`) is 2 calls.
    -   The IQR is 2 - 1 = 1 call. This very tight range indicates that the central 50% of customers make a very consistent and low number of calls to customer service.

-   **Whiskers (lines extending from the box):** The whiskers typically cover the data points that are not considered outliers. The minimum value is 0 calls, indicating some customers have never called customer service, and the maximum is 9 calls.

-   **Outliers (individual points beyond the whiskers):** The boxplot shows several outliers extending to higher values, up to a maximum of 9 calls. These outliers represent a small but significant group of customers who frequently contact customer service. These could be customers experiencing persistent issues or those with complex service needs, and this feature is often a strong indicator for churn.

****
"""

df.columns

"""# Cheking Collinearity

### Preprocessing Actions Based on Insights:

1.  **Type Casting for Numeric Columns**: The `df.info()` output and subsequent checks revealed that `day.charge` and `eve.mins` columns are currently of `object` type but should be numeric. These need to be converted to a suitable numeric type (e.g., `float`). The 'Nan' values identified in `eve.mins` and potential non-numeric entries in `day.charge` rows will need to be handled during this conversion (e.g., coerced to `NaN` and then imputed or dropped if necessary).

2.  **Categorical Feature Encoding**: While some binary categorical features (`voice.plan`, `intl.plan`, `churn`) have already been converted to numeric (0s and 1s), the `state` and `area.code` columns remain as `object` type. These will likely need to be encoded, possibly using one-hot encoding, especially if they are to be used in machine learning models.

3.  **Handling Outliers**: Outliers were identified in several numeric features (`account.length`, `voice.messages`, `intl.mins`, `intl.calls`, `intl.charge`, `day.mins`, `day.calls`, `eve.calls`, `eve.charge`, `night.mins`, `night.calls`, `night.charge`, `customer.calls`). The analysis suggested these are mostly valid extreme values rather than errors. Depending on the modeling approach, these might be left as is, transformed (e.g., logarithmic transformation for skewed data), or capped (winsorization) rather than removed.

4.  **Addressing Class Imbalance**: The `churn` column (target variable) shows an imbalanced distribution (more 'no' instances than 'yes'). This is an important consideration for model training. Techniques like oversampling (e.g., SMOTE), undersampling, or using algorithms robust to imbalance (e.g., tree-based models, weighted loss functions) should be explored during the modeling phase.

5.  **Duplicate Rows**: No duplicate rows were found in the dataset, so no action is required here.
"""







